version: "3.4"
services: 
  quaver:
    build:
      context: .
      args: 
        DEV_ENV: 1
      network: host
    volumes: 
      - ./:/app
      - ./music/:/music
    environment: 
      - DEV_ENV=1
    command: npm run dev